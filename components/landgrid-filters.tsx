'use client';

import { useEffect, useMemo, useState } from 'react';
import { Check, ChevronDown, ChevronRight, Search, X } from 'lucide-react';

type Owner = { id: string; name: string; color?: string | null };
type Lot = { id: string; number: string; ownerId?: string | null; ownerName?: string; status: string; area?: number | null; price?: number | string | null; direction?: string };
type OpenState = { owner: boolean; status: boolean; plots: boolean; search: boolean };
const STATUS: Record<string, { label: string; dot: string }> = { available: { label: 'Available', dot: '#16a34a' }, reserved: { label: 'Reserved', dot: '#ca8a04' }, sold: { label: 'Sold', dot: '#dc2626' }, hold: { label: 'Hold', dot: '#64748b' } };
const COLORS = ['#ef4444', '#facc15', '#e879f9', '#22c55e', '#d97732', '#38bdf8', '#a78bfa', '#14b8a6'];
const norm = (value: unknown) => String(value ?? '').trim().toLowerCase();
const colorFor = (owner: Owner, index: number) => owner.color || COLORS[index % COLORS.length];
function getMapSvg() { return Array.from(document.querySelectorAll<SVGSVGElement>('.pv-canvas svg')).find(svg => svg.querySelector('polygon')) || null; }
function getPlotNumber(group: SVGGElement) { const text = Array.from(group.querySelectorAll('text')).find(node => { const value = (node.textContent || '').trim(); return value && !node.hasAttribute('data-landgrid-yard') && !node.hasAttribute('data-landgrid-yard-label'); }); return text?.textContent?.trim() || ''; }

export default function LandGridFilters({ projectSlug }: { projectSlug: string }) {
  const [owners, setOwners] = useState<Owner[]>([]), [lots, setLots] = useState<Lot[]>([]);
  const [selectedOwners, setSelectedOwners] = useState<Set<string>>(() => new Set()), [selectedPlot, setSelectedPlot] = useState<Lot | null>(null);
  const [search, setSearch] = useState(''), [status, setStatus] = useState('all'), [mobileOpen, setMobileOpen] = useState(false);
  const [open, setOpen] = useState<OpenState>({ owner: true, status: false, plots: false, search: false });

  useEffect(() => { let cancelled = false; fetch(`/api/projects/${encodeURIComponent(projectSlug)}/plan`, { cache: 'no-store' }).then(response => { if (!response.ok) throw new Error(`Plan request failed: ${response.status}`); return response.json(); }).then(data => { if (cancelled) return; const rawOwners = Array.isArray(data?.owners) ? data.owners : Array.isArray(data?.project_owners) ? data.project_owners : []; const parsedOwners: Owner[] = rawOwners.map((owner: any) => ({ id: String(owner.id), name: String(owner.name || 'Unnamed owner'), color: owner.color || null })); const ownerNames = new Map(parsedOwners.map(owner => [owner.id, owner.name])); const rawLots = Array.isArray(data?.lots) ? data.lots : Array.isArray(data?.plots) ? data.plots : []; const parsedLots: Lot[] = rawLots.map((lot: any) => { const ownerId = lot.ownerId != null ? String(lot.ownerId) : lot.owner_id != null ? String(lot.owner_id) : null; return { id: String(lot.id), number: String(lot.number ?? lot.plot_number ?? ''), ownerId, ownerName: String(lot.ownerName || lot.owner_name || (ownerId ? ownerNames.get(ownerId) : '') || ''), status: norm(lot.status || 'available'), area: lot.area != null ? Number(lot.area) : lot.area_sq_yards != null ? Number(lot.area_sq_yards) : null, price: lot.price ?? null, direction: String(lot.direction || '') }; }); parsedOwners.sort((a, b) => a.name.localeCompare(b.name)); parsedLots.sort((a, b) => Number(a.number) - Number(b.number)); setOwners(parsedOwners.filter(owner => parsedLots.some(lot => lot.ownerId === owner.id))); setLots(parsedLots); }).catch(error => { console.error('[LandGrid] Failed to load plan', error); if (!cancelled) { setOwners([]); setLots([]); } }); return () => { cancelled = true; }; }, [projectSlug]);

  const ownerMap = useMemo(() => new Map(owners.map(owner => [owner.id, owner])), [owners]);
  const ownerIndex = useMemo(() => new Map(owners.map((owner, index) => [owner.id, index])), [owners]);
  const filteredLots = useMemo(() => { const query = norm(search); return lots.filter(lot => { const ownerName = norm(lot.ownerName || ownerMap.get(String(lot.ownerId))?.name); return (!query || norm(lot.number).includes(query) || ownerName.includes(query)) && (status === 'all' || lot.status === status); }); }, [lots, search, status, ownerMap]);
  const selectedLots = useMemo(() => lots.filter(lot => lot.ownerId != null && selectedOwners.has(String(lot.ownerId))), [lots, selectedOwners]);
  const selectedYards = useMemo(() => selectedLots.reduce((sum, lot) => sum + (Number.isFinite(Number(lot.area)) ? Number(lot.area) : 0), 0), [selectedLots]);
  const toggleOwner = (ownerId: string) => setSelectedOwners(previous => { const next = new Set(previous); if (next.has(ownerId)) next.delete(ownerId); else next.add(ownerId); return next; });
  const clearOwners = () => setSelectedOwners(new Set());
  const focusLot = (lot: Lot) => { setSelectedPlot(lot); const svg = getMapSvg(); if (!svg) return; const group = Array.from(svg.querySelectorAll<SVGGElement>('g')).find(item => getPlotNumber(item) === lot.number); if (group) group.dispatchEvent(new MouseEvent('click', { bubbles: true })); };
  const toggleSection = (section: keyof OpenState) => setOpen(previous => ({ ...previous, [section]: !previous[section] }));

  useEffect(() => { let cancelled = false, frame = 0; const apply = () => { if (cancelled) return; const svg = getMapSvg(); if (!svg) return; svg.querySelectorAll<SVGGElement>('g').forEach(group => { const number = getPlotNumber(group), lot = lots.find(item => item.number === number), polygon = group.querySelector<SVGPolygonElement>('polygon'); if (!lot || !polygon) return; const ownerId = lot.ownerId == null ? '' : String(lot.ownerId), owner = ownerMap.get(ownerId), active = ownerId !== '' && selectedOwners.has(ownerId); if (active) { const color = owner ? colorFor(owner, ownerIndex.get(owner.id) ?? 0) : '#2563eb'; polygon.style.setProperty('fill', color, 'important'); polygon.style.setProperty('fill-opacity', '0.92', 'important'); polygon.style.setProperty('stroke', '#fff', 'important'); polygon.style.setProperty('stroke-width', '4', 'important'); polygon.style.setProperty('filter', `drop-shadow(0 0 5px ${color})`, 'important'); } else { polygon.style.removeProperty('fill'); polygon.style.removeProperty('fill-opacity'); polygon.style.removeProperty('stroke'); polygon.style.removeProperty('stroke-width'); polygon.style.removeProperty('filter'); } }); }; const run = () => { cancelAnimationFrame(frame); frame = requestAnimationFrame(apply); }; let attempts = 0; const retry = () => { if (cancelled || attempts++ >= 12) return; run(); window.setTimeout(retry, 100); }; retry(); return () => { cancelled = true; cancelAnimationFrame(frame); }; }, [lots, selectedOwners, ownerMap, ownerIndex]);

  const summary = selectedOwners.size > 0 ? <div className="lg-mobile-summary"><div className="lg-summary-head"><div><strong>Owner Summary</strong><span>{selectedLots.length} plots · {selectedYards.toLocaleString(undefined, { maximumFractionDigits: 2 })} sq.yd</span></div><button type="button" onClick={clearOwners}>Clear</button></div>{owners.filter(owner => selectedOwners.has(owner.id)).map(owner => { const ownerLots = lots.filter(lot => lot.ownerId === owner.id), yards = ownerLots.reduce((sum, lot) => sum + (Number.isFinite(Number(lot.area)) ? Number(lot.area) : 0), 0), index = ownerIndex.get(owner.id) ?? 0; return <div className="lg-owner-card" key={owner.id}><div className="lg-owner-card-title"><span className="lg-dot" style={{ background: colorFor(owner, index) }} />{owner.name}</div><div className="lg-owner-total">{ownerLots.length} plots · {yards.toLocaleString(undefined, { maximumFractionDigits: 2 })} sq.yd</div><div className="lg-owner-plots">{ownerLots.map(lot => <button key={lot.id} type="button" onClick={() => focusLot(lot)}>Plot {lot.number}<span>{lot.area != null ? `${lot.area.toLocaleString()} yd²` : '—'}</span></button>)}</div></div>; })}</div> : null;

  return <div className="lg-shell-overlay">
    <aside className={`lg-left-panel ${mobileOpen ? 'mobile-open' : ''}`} aria-label="Filters and find">
      <button type="button" className="lg-mobile-toggle" onClick={() => setMobileOpen(value => !value)} aria-expanded={mobileOpen} aria-label={mobileOpen ? 'Collapse filters' : 'Open filters'}><span className="lg-mobile-handle" /><strong>Filters &amp; Find</strong><span className="lg-mobile-chevron">{mobileOpen ? '⌄' : '⌃'}</span></button>
      <div className="lg-title"><strong>Filters &amp; Find</strong><span>Select what you want to see on the map.</span></div>
      <section className="lg-section"><button type="button" className="lg-heading" onClick={() => toggleSection('owner')}><span className="lg-heading-left">{open.owner ? <ChevronDown size={15} /> : <ChevronRight size={15} />}<b>Filter by Owner</b></span>{selectedOwners.size > 0 && <span className="lg-count">{selectedOwners.size}</span>}</button>{open.owner && <div className="lg-body"><div className="lg-owner-actions"><span>{selectedOwners.size ? `${selectedLots.length} plots selected` : 'Select one or more owners'}</span>{selectedOwners.size > 0 && <button type="button" onClick={clearOwners}>Clear</button>}</div>{owners.map((owner, index) => { const active = selectedOwners.has(owner.id); return <button key={owner.id} type="button" className={`lg-row lg-owner ${active ? 'active' : ''}`} aria-pressed={active} onClick={() => toggleOwner(owner.id)}><span className="lg-dot" style={{ background: colorFor(owner, index) }} /><span>{owner.name}</span><span className="lg-check" aria-hidden="true">{active ? <Check size={12} /> : null}</span></button>; })}{owners.length === 0 && <div className="lg-empty">No plot owners assigned.</div>}</div>}</section>
      {summary}
      <section className="lg-section"><button type="button" className="lg-heading" onClick={() => toggleSection('status')}><span className="lg-heading-left">{open.status ? <ChevronDown size={15} /> : <ChevronRight size={15} />}<b>Plot Status</b></span></button>{open.status && <div className="lg-body"><div className="lg-status-grid">{[['all', 'All'], ...Object.entries(STATUS).map(([key, value]) => [key, value.label])].map(([key, label]) => <button key={key} type="button" className={`lg-status ${status === key ? 'active' : ''}`} onClick={() => setStatus(key)}><span className="lg-dot" style={{ background: key === 'all' ? '#94a3b8' : STATUS[key].dot }} /><span>{label}</span><em>{key === 'all' ? lots.length : lots.filter(lot => lot.status === key).length}</em></button>)}</div></div>}</section>
      <section className="lg-section"><button type="button" className="lg-heading" onClick={() => toggleSection('plots')}><span className="lg-heading-left">{open.plots ? <ChevronDown size={15} /> : <ChevronRight size={15} />}<b>Plots</b></span><span className="lg-count">{filteredLots.length}</span></button>{open.plots && <div className="lg-body"><div className="lg-plot-list">{filteredLots.map(lot => <button key={lot.id} type="button" className={`lg-row lg-plot ${selectedPlot?.id === lot.id ? 'active' : ''}`} onClick={() => focusLot(lot)}><span className="lg-dot" style={{ background: STATUS[lot.status]?.dot || '#64748b' }} /><span><b>Plot {lot.number}</b><small>{lot.ownerName || ownerMap.get(String(lot.ownerId))?.name || 'No owner'}{lot.area != null ? ` · ${lot.area.toLocaleString()} sq.yd` : ''}</small></span></button>)}{filteredLots.length === 0 && <div className="lg-empty">No plots match the current filters.</div>}</div></div>}</section>
      <section className="lg-section"><button type="button" className="lg-heading" onClick={() => toggleSection('search')}><span className="lg-heading-left">{open.search ? <ChevronDown size={15} /> : <ChevronRight size={15} />}<b>Free Search</b></span>{search && <span className="lg-count">1</span>}</button>{open.search && <div className="lg-body"><div className="lg-search-wrap"><Search size={14} /><input value={search} onChange={event => setSearch(event.target.value)} placeholder="Plot number or owner name" aria-label="Search plot or owner" />{search && <button type="button" onClick={() => setSearch('')}><X size={13} /></button>}</div></div>}</section>
    </aside>
    <aside className="lg-right-panel" aria-label="Property information"><div className="lg-title"><strong>{selectedOwners.size ? 'Owner Plot Summary' : selectedPlot ? `Plot ${selectedPlot.number}` : 'Property Information'}</strong><span>{selectedOwners.size ? `${selectedLots.length} plots · ${selectedYards.toLocaleString(undefined, { maximumFractionDigits: 2 })} sq.yd` : selectedPlot ? 'Selected property' : 'Select an owner or plot to view details.'}</span></div>{summary ?? (selectedPlot ? <div className="lg-detail"><div className="lg-detail-stat"><small>Status</small><b>{STATUS[selectedPlot.status]?.label || selectedPlot.status}</b></div><div className="lg-detail-stat"><small>Owner</small><b>{selectedPlot.ownerName || ownerMap.get(String(selectedPlot.ownerId))?.name || 'Unassigned'}</b></div>{selectedPlot.area != null && <div className="lg-detail-stat"><small>Area</small><b>{selectedPlot.area.toLocaleString()} sq.yd</b></div>}{selectedPlot.price != null && <div className="lg-detail-stat"><small>Price</small><b>{String(selectedPlot.price)}</b></div>}{selectedPlot.direction && <div className="lg-detail-stat"><small>Direction</small><b>{selectedPlot.direction}</b></div>}</div> : <div className="lg-empty lg-empty-large">Use <b>Filter by Owner</b> to select one or more owners and highlight their plots on the map.</div>)}</aside>
    <style>{`
      .lg-shell-overlay{position:absolute;inset:0;z-index:1000;pointer-events:none}.lg-left-panel,.lg-right-panel{position:absolute;top:0;bottom:0;pointer-events:auto;background:#fff;border:1px solid #dbe3ee;box-shadow:0 8px 28px rgba(15,23,42,.16);overflow:auto;font-family:Inter,ui-sans-serif,system-ui,sans-serif;color:#172033}.lg-left-panel{left:0;width:280px}.lg-right-panel{right:0;width:320px}.lg-left-panel *,.lg-right-panel *{box-sizing:border-box}.lg-title{padding:14px 15px;border-bottom:1px solid #e2e8f0}.lg-title strong{display:block;font-size:14px;font-weight:900}.lg-title span{display:block;margin-top:3px;font-size:10px;color:#64748b;line-height:1.45}.lg-section{border-bottom:1px solid #e2e8f0}.lg-heading{width:100%;padding:12px 14px;border:0;background:#fff;display:flex;align-items:center;justify-content:space-between;cursor:pointer;color:#172033;text-align:left}.lg-heading-left{display:flex;align-items:center;gap:5px}.lg-count{min-width:20px;padding:2px 6px;border-radius:99px;background:#172554;color:#fff;text-align:center;font-size:10px;font-weight:800}.lg-body{padding:0 10px 10px}.lg-owner-actions{display:flex;justify-content:space-between;align-items:center;padding:2px 3px 4px;color:#64748b;font-size:10px}.lg-owner-actions button,.lg-summary-head button{border:0;background:none;color:#334155;font-size:10px;font-weight:800;cursor:pointer}.lg-row,.lg-status{width:100%;border:1px solid #e2e8f0;background:#fff;border-radius:8px;margin-top:5px;cursor:pointer;text-align:left}.lg-row{display:flex;align-items:center;gap:8px;padding:8px 9px}.lg-row.active{background:#f0f4ff;border-color:#818cf8}.lg-owner span:nth-child(2){flex:1;font-size:12px;font-weight:700;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.lg-dot{width:10px;height:10px;border-radius:50%;flex:none}.lg-check{width:18px;height:18px;border-radius:5px;border:1px solid #cbd5e1;display:grid;place-items:center;color:#fff}.lg-owner.active .lg-check{background:#172554;border-color:#172554}.lg-status-grid{display:grid;grid-template-columns:1fr 1fr;gap:6px}.lg-status{padding:8px 9px;display:flex;align-items:center;gap:7px;color:#243047}.lg-status.active{background:#172554;color:#fff;border-color:#172554}.lg-status em{margin-left:auto;font-style:normal;font-size:10px;opacity:.7}.lg-plot-list{max-height:330px;overflow:auto}.lg-plot small{display:block;margin-top:2px;color:#64748b;font-size:10px}.lg-plot b{font-size:12px}.lg-empty{padding:12px 3px;color:#94a3b8;font-size:11px}.lg-empty-large{padding:36px 24px;text-align:center;line-height:1.7}.lg-search-wrap{position:relative;display:flex;align-items:center}.lg-search-wrap>svg{position:absolute;left:9px;color:#94a3b8}.lg-search-wrap input{width:100%;border:1px solid #dbe2ea;border-radius:8px;padding:9px 30px;font-size:12px;outline:none}.lg-search-wrap button{position:absolute;right:4px;border:0;background:none;cursor:pointer;color:#64748b}.lg-summary-head{display:flex;justify-content:space-between;align-items:center;padding:10px 12px;border-bottom:1px solid #e2e8f0}.lg-summary-head strong{display:block;font-size:13px}.lg-summary-head span{display:block;margin-top:2px;font-size:10px;color:#64748b}.lg-mobile-summary{border-bottom:1px solid #e2e8f0;background:#f8fafc}.lg-owner-card{margin:10px 12px;border:1px solid #dbe3ee;border-radius:10px;padding:11px;background:#fff}.lg-owner-card-title{display:flex;align-items:center;gap:7px;font-size:13px;font-weight:900}.lg-owner-total{margin-top:7px;font-size:12px;font-weight:800;color:#334155}.lg-owner-plots{display:grid;grid-template-columns:1fr 1fr;gap:4px;margin-top:7px;max-height:180px;overflow:auto}.lg-owner-plots button{border:1px solid #edf1f6;background:#f8fafc;border-radius:6px;padding:5px 6px;text-align:left;font-size:10px;cursor:pointer}.lg-owner-plots span{display:block;color:#64748b;margin-top:2px}.lg-detail{padding:12px}.lg-detail-stat{padding:11px 12px;margin-bottom:8px;border:1px solid #e2e8f0;border-radius:9px;background:#f8fafc}.lg-detail-stat small{display:block;color:#64748b;font-size:9px;font-weight:800;text-transform:uppercase}.lg-detail-stat b{display:block;margin-top:3px;font-size:14px}.lg-mobile-toggle{display:none}
      @media(max-width:767px){.lg-shell-overlay{position:fixed;inset:0;z-index:1000;pointer-events:none}.lg-left-panel{left:8px;right:8px;top:auto;bottom:max(8px,env(safe-area-inset-bottom));width:auto;height:58px;max-height:58px;overflow:hidden;border-radius:16px;box-shadow:0 8px 28px rgba(15,23,42,.28);transition:max-height .22s ease}.lg-left-panel.mobile-open{height:auto;max-height:min(78dvh,650px);overflow:auto}.lg-mobile-toggle{display:flex;width:100%;height:58px;align-items:center;gap:10px;padding:6px 14px;border:0;background:#fff;color:#172033;text-align:left;cursor:pointer}.lg-mobile-toggle strong{flex:1;font-size:13px}.lg-mobile-handle{width:34px;height:4px;border-radius:99px;background:#cbd5e1}.lg-mobile-chevron{font-size:18px;color:#64748b}.lg-left-panel>.lg-title{display:none}.lg-left-panel .lg-section{display:none}.lg-left-panel.mobile-open .lg-section{display:block}.lg-left-panel .lg-heading{min-height:44px;padding:10px 14px}.lg-left-panel .lg-body{max-height:34dvh;overflow:auto;-webkit-overflow-scrolling:touch}.lg-left-panel .lg-row{min-height:42px;padding:9px 14px}.lg-left-panel .lg-owner-actions{padding:8px 14px}.lg-right-panel{display:none}.lg-left-panel input{font-size:16px}.lg-mobile-summary{display:block}.lg-mobile-summary .lg-owner-plots{max-height:140px}}
    `}</style>
  </div>;
}
