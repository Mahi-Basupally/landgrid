"use client";

import { useEffect, useMemo, useRef, useState } from "react";
import { Minus, Plus, Save, Trash2, Upload, Copy, X } from "lucide-react";

type Point={x:number;y:number};
type Layer={visible:boolean;opacity:number;x:number;y:number;width:number;height:number};
type Layers={map:Layer;drone:Layer};
type Plan={id:string;name:string;sortOrder?:number;masterPlanUrl?:string|null;droneUrl?:string|null;points?:string|null;layerGeometry?:any};
type Plot={id:string;number:string;status:string;owner:string;price:number|string|null;area:number|string|null;direction:string;model:string;points:string;labelX:number;labelY:number;sectionId?:string|null};
type ShapeMode="rectangle"|"polygon";
type ResizeHandle="nw"|"n"|"ne"|"e"|"se"|"s"|"sw"|"w";
type Drag={kind:"pan"|"move"|"point"|"resize";id:string;start:Point;points?:Point[];index?:number;handle?:ResizeHandle;panStart?:Point};

const W=1600,H=1000;
const parse=(s:string):Point[]=>s.trim().split(/\s+/).filter(Boolean).map(v=>v.split(",").map(Number)).filter(v=>Number.isFinite(v[0])&&Number.isFinite(v[1])).map(([x,y])=>({x,y}));
const stringify=(p:Point[])=>p.map(v=>`${Math.round(v.x)},${Math.round(v.y)}`).join(" ");
const center=(p:Point[])=>p.length?{x:p.reduce((a,v)=>a+v.x,0)/p.length,y:p.reduce((a,v)=>a+v.y,0)/p.length}:{x:W/2,y:H/2};
const bounds=(p:Point[])=>!p.length?{minX:0,minY:0,maxX:W,maxY:H}:{minX:Math.min(...p.map(v=>v.x)),minY:Math.min(...p.map(v=>v.y)),maxX:Math.max(...p.map(v=>v.x)),maxY:Math.max(...p.map(v=>v.y))};
const rectangle=(p:Point[]):Point[]=>{const b=bounds(p);return[{x:b.minX,y:b.minY},{x:b.maxX,y:b.minY},{x:b.maxX,y:b.maxY},{x:b.minX,y:b.maxY}]};
const polygon8=(p:Point[]):Point[]=>{const b=bounds(p),mx=(b.minX+b.maxX)/2,my=(b.minY+b.maxY)/2;return[{x:b.minX,y:b.minY},{x:mx,y:b.minY},{x:b.maxX,y:b.minY},{x:b.maxX,y:my},{x:b.maxX,y:b.maxY},{x:mx,y:b.maxY},{x:b.minX,y:b.maxY},{x:b.minX,y:my}]};
const normalize=(p:Point[])=>p.length===4||p.length===8?p:p.length>=3?p:rectangle([{x:600,y:400},{x:800,y:400},{x:800,y:520},{x:600,y:520}]);
const defaultLayer=(opacity:number):Layer=>({visible:true,opacity,x:0,y:0,width:W,height:H});
const defaultLayers=():Layers=>({map:defaultLayer(.72),drone:defaultLayer(.42)});
const handlePoints=(p:Point[])=>{const b=bounds(p),mx=(b.minX+b.maxX)/2,my=(b.minY+b.maxY)/2;return[{x:b.minX,y:b.minY,h:"nw" as ResizeHandle},{x:mx,y:b.minY,h:"n" as ResizeHandle},{x:b.maxX,y:b.minY,h:"ne" as ResizeHandle},{x:b.maxX,y:my,h:"e" as ResizeHandle},{x:b.maxX,y:b.maxY,h:"se" as ResizeHandle},{x:mx,y:b.maxY,h:"s" as ResizeHandle},{x:b.minX,y:b.maxY,h:"sw" as ResizeHandle},{x:b.minX,y:my,h:"w" as ResizeHandle}]};

export default function LotEditor({projectSlug}:{projectSlug:string}){
 const [plans,setPlans]=useState<Plan[]>([]),[plots,setPlots]=useState<Plot[]>([]),[sections,setSections]=useState<Record<string,string>>({});
 const [planId,setPlanId]=useState("master_plan"),[selected,setSelected]=useState<string|null>(null),[selectedSection,setSelectedSection]=useState<string|null>(null),[shapeMode,setShapeMode]=useState<ShapeMode>("polygon");
 const [layers,setLayers]=useState<Layers>(defaultLayers()),[zoom,setZoom]=useState(1),[pan,setPan]=useState<Point>({x:0,y:0}),[drag,setDrag]=useState<Drag|null>(null),[dirty,setDirty]=useState(false),[busy,setBusy]=useState(false),[message,setMessage]=useState("");
 const [confirm,setConfirm]=useState<{kind:"plot"|"section";id:string;label:string}|null>(null),[uploading,setUploading]=useState<"map"|"drone"|null>(null);
 const svgRef=useRef<SVGSVGElement|null>(null),mapInput=useRef<HTMLInputElement|null>(null),droneInput=useRef<HTMLInputElement|null>(null),loaded=useRef(false),saveTimer=useRef<ReturnType<typeof setTimeout>|null>(null);
 const master=plans.find(p=>p.id==="master_plan")||plans[0],section=plans.find(p=>p.id===selectedSection)||null,selectedPlot=plots.find(p=>p.id===selected)||null;
 const sectionList=useMemo(()=>plans.filter(p=>p.id!=="master_plan"),[plans]);
 const visiblePlots=useMemo(()=>planId==="master_plan"?plots:plots.filter(p=>p.sectionId===planId),[plots,planId]);
 const viewW=W/zoom,viewH=H/zoom,viewBox=`${(W-viewW)/2+pan.x} ${(H-viewH)/2+pan.y} ${viewW} ${viewH}`;
 const point=(e:React.PointerEvent<SVGElement>):Point=>{const svg=svgRef.current;if(!svg)return{x:0,y:0};const r=svg.getBoundingClientRect(),v=svg.viewBox.baseVal;return{x:v.x+(e.clientX-r.left)/r.width*v.width,y:v.y+(e.clientY-r.top)/r.height*v.height}};
 const asset=(kind:"map"|"drone")=>{const url=kind==="map"?master?.masterPlanUrl:master?.droneUrl;if(!url)return null;return`/api/projects/${encodeURIComponent(projectSlug)}/assets/file?kind=${kind==="map"?"master-plan":"drone"}&planType=master_plan&v=${encodeURIComponent(url)}`};
 useEffect(()=>{void load()},[projectSlug]);
 useEffect(()=>{if(!loaded.current||!dirty)return;if(saveTimer.current)clearTimeout(saveTimer.current);saveTimer.current=setTimeout(()=>void save(),700);return()=>{if(saveTimer.current)clearTimeout(saveTimer.current)}},[plans,plots,sections,layers,zoom,pan,dirty]);
 async function load(){try{const r=await fetch(`/api/projects/${encodeURIComponent(projectSlug)}/plan`,{cache:"no-store"}),d=await r.json();if(!r.ok)throw new Error(d.error||"Unable to load plan");const ps=(d.sections||[]) as Plan[];setPlans(ps);const ss:Record<string,string>={};ps.filter(p=>p.id!=="master_plan").forEach(p=>ss[p.id]=p.points||"");setSections(ss);setPlots((d.lots||[]).map((p:Plot)=>{const q=normalize(parse(p.points||"")),c=center(q);return{...p,points:stringify(q),labelX:c.x,labelY:c.y}}));const g=ps.find(p=>p.id==="master_plan")?.layerGeometry;if(g){if(g.map)setLayers(v=>({...v,map:{...defaultLayer(.72),...g.map}}));if(g.drone)setLayers(v=>({...v,drone:{...defaultLayer(.42),...g.drone}}));if(Number.isFinite(g.zoom))setZoom(Math.max(.25,Math.min(8,Number(g.zoom))));setPan({x:Number(g.panX||0),y:Number(g.panY||0)})}loaded.current=true}catch(e){setMessage(e instanceof Error?e.message:"Unable to load plan")}}
 async function save(){if(!loaded.current||busy||!dirty)return;setBusy(true);try{const layerGeometry={map:layers.map,drone:layers.drone,zoom,panX:pan.x,panY:pan.y};const payload=plans.map(p=>({...p,points:p.id==="master_plan"?p.points||null:sections[p.id]||p.points||null,layerGeometry:p.id==="master_plan"?layerGeometry:p.layerGeometry||{}}));const r=await fetch(`/api/projects/${encodeURIComponent(projectSlug)}/plan`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({sections:payload,lots:plots})}),d=await r.json();if(!r.ok)throw new Error(d.error||"Unable to save");setDirty(false);setMessage("Saved to database")}catch(e){setMessage(e instanceof Error?e.message:"Unable to save")}finally{setBusy(false)}}
 function deselect(){setSelected(null);setSelectedSection(null);setDrag(null)}
 async function addSection(){const n=Math.max(0,...plans.map(p=>Number(p.id.match(/^section_(\d+)$/)?.[1]||0)))+1,id=`section_${n}`;try{const r=await fetch(`/api/projects/${encodeURIComponent(projectSlug)}/plans`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({planType:id})}),d=await r.json();if(!r.ok)throw new Error(d.error||"Unable to add section");const q=stringify(polygon8([{x:120+((n-1)%3)*480,y:120+Math.floor((n-1)/3)*300},{x:500+((n-1)%3)*480,y:120+Math.floor((n-1)/3)*300},{x:500+((n-1)%3)*480,y:350+Math.floor((n-1)/3)*300},{x:120+((n-1)%3)*480,y:350+Math.floor((n-1)/3)*300}]));setPlans(v=>[...v,{id,name:`Section ${n}`,sortOrder:n,points:q}]);setSections(v=>({...v,[id]:q}));setPlanId(id);setSelectedSection(id);setSelected(null);setShapeMode("polygon");setDirty(true);setMessage(`Section ${n} added`)}catch(e){setMessage(e instanceof Error?e.message:"Unable to add section")}}
 async function deleteSection(id:string){try{const r=await fetch(`/api/projects/${encodeURIComponent(projectSlug)}/plans`,{method:"DELETE",headers:{"Content-Type":"application/json"},body:JSON.stringify({planType:id})}),d=await r.json();if(!r.ok)throw new Error(d.error||"Unable to delete section");setPlans(v=>v.filter(p=>p.id!==id));setSections(v=>{const n={...v};delete n[id];return n});setPlots(v=>v.map(p=>p.sectionId===id?{...p,sectionId:null}:p));setPlanId("master_plan");deselect();setDirty(true);setMessage("Section deleted")}catch(e){setMessage(e instanceof Error?e.message:"Unable to delete section")}finally{setConfirm(null)}}
 function renamePlan(id:string,name:string){setPlans(v=>v.map(p=>p.id===id?{...p,name}:p));setDirty(true)}
 function addPlot(){const n=String(Math.max(0,...plots.map(p=>Number(p.number)).filter(Number.isFinite))+1),sectionId=planId==="master_plan"?null:planId,q=polygon8([{x:650,y:430},{x:830,y:430},{x:830,y:540},{x:650,y:540}]),c=center(q),p:Plot={id:crypto.randomUUID(),number:n,status:"available",owner:"",price:null,area:null,direction:"",model:"",points:stringify(q),labelX:c.x,labelY:c.y,sectionId};setPlots(v=>[...v,p]);setSelected(p.id);setSelectedSection(null);setShapeMode("polygon");setDirty(true);setMessage(`Plot ${n} added${sectionId?` to ${plans.find(s=>s.id===sectionId)?.name||"Section"}`:""}`)}
 function duplicatePlot(){if(!selectedPlot)return;const q=parse(selectedPlot.points).map(p=>({x:p.x+40,y:p.y+40})),c=center(q);const base=Number(selectedPlot.number),n=Number.isFinite(base)?String(Math.max(...plots.map(p=>Number(p.number)).filter(Number.isFinite),base)+1):`${selectedPlot.number}-copy`;const p:Plot={...selectedPlot,id:crypto.randomUUID(),number:n,points:stringify(q),labelX:c.x,labelY:c.y};setPlots(v=>[...v,p]);setSelected(p.id);setSelectedSection(null);setDirty(true);setMessage(`Plot ${n} duplicated`)}
 function updatePlot(k:keyof Plot,value:string){if(!selectedPlot)return;setPlots(v=>v.map(p=>p.id===selectedPlot.id?{...p,[k]:k==="sectionId"?(value||null):value}:p));setDirty(true)}
 function choosePlot(id:string){if(!id){setSelected(null);return}const p=plots.find(x=>x.id===id);if(!p)return;const q=normalize(parse(p.points)),c=center(q),b=bounds(q),target=Math.min(8,Math.max(.75,Math.min(W/Math.max(1,b.maxX-b.minX),H/Math.max(1,b.maxY-b.minY))*.55));setSelected(id);setSelectedSection(null);setPlanId(p.sectionId||"master_plan");setShapeMode(q.length===4?"rectangle":"polygon");setZoom(target);setPan({x:c.x-W/2,y:c.y-H/2})}
 function selectSection(id:string){setPlanId(id);setSelectedSection(id);setSelected(null);const q=normalize(parse(sections[id]||""));setShapeMode(q.length===4?"rectangle":"polygon");if(q.length){const c=center(q),b=bounds(q);setPan({x:c.x-W/2,y:c.y-H/2});setZoom(Math.min(8,Math.max(.6,Math.min(W/Math.max(1,b.maxX-b.minX),H/Math.max(1,b.maxY-b.minY))*.75)))}}
 function convertSelected(mode:ShapeMode){const target=selectedPlot?.points||(selectedSection?sections[selectedSection]:"");const q=parse(target),n=mode==="rectangle"?rectangle(q):polygon8(q),c=center(n);if(selectedPlot)setPlots(v=>v.map(p=>p.id===selectedPlot.id?{...p,points:stringify(n),labelX:c.x,labelY:c.y}:p));if(selectedSection){const s=stringify(n);setSections(v=>({...v,[selectedSection]:s}));setPlans(v=>v.map(p=>p.id===selectedSection?{...p,points:s}:p))}setShapeMode(mode);setDirty(true)}
 function startCanvasPan(e:React.PointerEvent<SVGSVGElement>){if(e.button!==0)return;setSelected(null);setSelectedSection(null);setDrag({kind:"pan",id:"canvas",start:{x:e.clientX,y:e.clientY},panStart:{...pan}});e.currentTarget.setPointerCapture(e.pointerId)}
 function startShape(e:React.PointerEvent<SVGElement>,id:string,q:Point[],kind:"plot"|"section"){e.stopPropagation();if(kind==="plot"){setSelected(id);setSelectedSection(null)}else{setSelectedSection(id);setSelected(null)}setShapeMode(q.length===4?"rectangle":"polygon");setDrag({kind:"move",id,start:point(e),points:q});e.currentTarget.setPointerCapture(e.pointerId)}
 function startPoint(e:React.PointerEvent<SVGCircleElement>,id:string,index:number,kind:"plot"|"section"){e.stopPropagation();const q=normalize(kind==="plot"?parse(plots.find(p=>p.id===id)?.points||""):parse(sections[id]||""));if(kind==="plot")setSelected(id);else setSelectedSection(id);setDrag({kind:"point",id,start:point(e),points:q,index});e.currentTarget.setPointerCapture(e.pointerId)}
 function startResize(e:React.PointerEvent<SVGCircleElement>,id:string,handle:ResizeHandle,kind:"plot"|"section"){e.stopPropagation();const q=normalize(kind==="plot"?parse(plots.find(p=>p.id===id)?.points||""):parse(sections[id]||""));if(kind==="plot")setSelected(id);else setSelectedSection(id);setDrag({kind:"resize",id,start:point(e),points:q,handle});e.currentTarget.setPointerCapture(e.pointerId)}
 function resizePoints(q:Point[],handle:ResizeHandle,dx:number,dy:number):Point[]{const b=bounds(q),min=30;let minX=b.minX,maxX=b.maxX,minY=b.minY,maxY=b.maxY;if(handle.includes("w"))minX=Math.min(b.maxX-min,minX+dx);if(handle.includes("e"))maxX=Math.max(b.minX+min,maxX+dx);if(handle.includes("n"))minY=Math.min(b.maxY-min,minY+dy);if(handle.includes("s"))maxY=Math.max(b.minY+min,maxY+dy);const oldW=Math.max(1,b.maxX-b.minX),oldH=Math.max(1,b.maxY-b.minY);const nw=maxX-minX,nh=maxY-minY;return q.map(p=>({x:minX+(p.x-b.minX)/oldW*nw,y:minY+(p.y-b.minY)/oldH*nh}))}
 function move(e:React.PointerEvent<SVGSVGElement>){if(!drag)return;if(drag.kind==="pan"){const r=svgRef.current?.getBoundingClientRect();if(!r)return;setPan({x:drag.panStart!.x-(e.clientX-drag.start.x)*viewW/r.width,y:drag.panStart!.y-(e.clientY-drag.start.y)*viewH/r.height});setDirty(true);return}const n=point(e),dx=n.x-drag.start.x,dy=n.y-drag.start.y;let q=drag.points!;if(drag.kind==="resize")q=resizePoints(q,drag.handle!,dx,dy);else q=q.map((p,i)=>drag.kind==="point"?(i===drag.index?{x:p.x+dx,y:p.y+dy}:p):{x:p.x+dx,y:p.y+dy});if(selected&&drag.id===selected){const c=center(q);setPlots(v=>v.map(p=>p.id===drag.id?{...p,points:stringify(q),labelX:c.x,labelY:c.y}:p))}else{const s=stringify(q);setSections(v=>({...v,[drag.id]:s}));setPlans(v=>v.map(p=>p.id===drag.id?{...p,points:s}:p))}setDirty(true)}
 async function upload(kind:"map"|"drone",file:File){setUploading(kind);try{const form=new FormData();form.append("file",file);form.append("kind",kind==="map"?"master-plan":"drone");form.append("planType","master_plan");const r=await fetch(`/api/projects/${encodeURIComponent(projectSlug)}/assets`,{method:"POST",body:form}),d=await r.json();if(!r.ok)throw new Error(d.error||"Upload failed");setPlans(v=>v.map(p=>p.id==="master_plan"?{...p,...(kind==="map"?{masterPlanUrl:d.savedValue}:{droneUrl:d.savedValue})}:p));setDirty(true);setMessage(`${kind} uploaded`)}catch(e){setMessage(e instanceof Error?e.message:"Upload failed")}finally{setUploading(null)}}
 function resetView(){setZoom(1);setPan({x:0,y:0});setDirty(true)}
 function layerChange(kind:"map"|"drone",patch:Partial<Layer>){setLayers(v=>({...v,[kind]:{...v[kind],...patch}}));setDirty(true)}
 const image=(kind:"map"|"drone")=>{const src=asset(kind);if(!src||!layers[kind].visible)return null;const l=layers[kind];return <image key={kind} href={src} x={l.x} y={l.y} width={l.width} height={l.height} opacity={l.opacity} preserveAspectRatio="none" pointerEvents="none"/>};
 const removePlot=()=>{if(!selectedPlot)return;setPlots(v=>v.filter(p=>p.id!==selectedPlot.id));setSelected(null);setDirty(true);setMessage("Plot deleted")};
 return <div style={{height:"100vh",display:"grid",gridTemplateRows:"68px 1fr",background:"#f3f5f8",color:"#172033",fontFamily:"Inter,system-ui,sans-serif"}}>
  <header style={{display:"flex",alignItems:"center",justifyContent:"space-between",padding:"0 18px",background:"white",borderBottom:"1px solid #e2e8f0"}}><div><small style={{fontWeight:800,color:"#64748b"}}>LANDGRID / MAP & MANAGE</small><h2 style={{margin:0,fontSize:20}}>{section?.name||master?.name||"Master Plan"}</h2></div><div style={{display:"flex",gap:8,alignItems:"center"}}><span style={{fontSize:12,color:busy?"#b45309":"#15803d"}}>{busy?"Saving…":message||"All changes saved"}</span><button type="button" onClick={()=>void save()} disabled={Boolean(!dirty||busy)}><Save size={14}/> Save</button></div></header>
  <div style={{display:"grid",gridTemplateColumns:"285px minmax(0,1fr) 310px",minHeight:0}}>
   <aside style={{background:"white",borderRight:"1px solid #e2e8f0",padding:12,overflow:"auto"}}><button type="button" onClick={()=>{setPlanId("master_plan");deselect()}} style={{width:"100%",padding:9,background:planId==="master_plan"?"#eef2ff":"white"}}>Master Plan</button><div style={{margin:"16px 0 6px",fontSize:11,fontWeight:900,color:"#64748b"}}>PLOTS</div><button type="button" onClick={addPlot} style={{width:"100%",padding:9}}><Plus size={14}/> Add Plot</button><select aria-label="Select a plot" value={selected||""} onChange={e=>choosePlot(e.target.value)} style={{width:"100%",marginTop:7,padding:9}}><option value="">Select a plot…</option>{plots.slice().sort((a,b)=>Number(a.number)-Number(b.number)).map(p=><option key={p.id} value={p.id}>Plot {p.number}{p.sectionId?` — ${plans.find(s=>s.id===p.sectionId)?.name||"Section"}`:" — Master Plan"}</option>)}</select><div style={{margin:"16px 0 6px",fontSize:11,fontWeight:900,color:"#64748b"}}>SECTIONS</div><button type="button" onClick={()=>void addSection()} style={{width:"100%",padding:9}}><Plus size={14}/> Add Section</button>{sectionList.map(s=><div key={s.id} style={{display:"flex",gap:4,marginTop:6}}><button type="button" onClick={()=>selectSection(s.id)} style={{flex:1,textAlign:"left",padding:8,background:selectedSection===s.id?"#dbeafe":"white"}}>{s.name}</button><button type="button" title="Delete section" onClick={()=>setConfirm({kind:"section",id:s.id,label:s.name})}><Trash2 size={14}/></button></div>)}<div style={{margin:"18px 0 6px",fontSize:11,fontWeight:900,color:"#64748b"}}>MAP / DRONE</div>{(["map","drone"] as const).map(k=><div key={k} style={{border:"1px solid #e2e8f0",borderRadius:8,padding:8,marginTop:6}}><b style={{fontSize:12}}>{k.toUpperCase()}</b><div style={{display:"flex",gap:5,marginTop:6}}><button type="button" onClick={()=>layerChange(k,{visible:!layers[k].visible})}>{layers[k].visible?"Hide":"Show"}</button><input aria-label={`${k} opacity`} type="range" min="0" max="1" step=".05" value={layers[k].opacity} onChange={e=>layerChange(k,{opacity:Number(e.target.value)})}/></div><button type="button" style={{width:"100%",marginTop:5}} onClick={()=>k==="map"?mapInput.current?.click():droneInput.current?.click()}><Upload size={13}/> {uploading===k?"Uploading…":`Upload ${k}`}</button></div>)}<input ref={mapInput} hidden type="file" accept="image/*,.svg" onChange={e=>{const f=e.target.files?.[0];if(f)void upload("map",f);e.currentTarget.value=""}}/><input ref={droneInput} hidden type="file" accept="image/*,.svg" onChange={e=>{const f=e.target.files?.[0];if(f)void upload("drone",f);e.currentTarget.value=""}}/></aside>
   <main style={{position:"relative",minWidth:0,background:"#dfe4e9"}}><div style={{position:"absolute",zIndex:5,top:12,left:12,right:12,display:"flex",gap:7,alignItems:"center",flexWrap:"wrap",padding:9,borderRadius:10,background:"rgba(255,255,255,.96)",boxShadow:"0 2px 12px rgba(0,0,0,.08)"}}><b>Canvas</b><button type="button" onClick={()=>setZoom(z=>Math.min(8,+(z*1.25).toFixed(2)))}><Plus size={14}/></button><span>{Math.round(zoom*100)}%</span><button type="button" onClick={()=>setZoom(z=>Math.max(.25,+(z/1.25).toFixed(2)))}><Minus size={14}/></button><button type="button" onClick={resetView}>Fit</button><span style={{marginLeft:"auto",color:"#64748b",fontSize:12}}>Drag anywhere to pan • blank click deselects</span></div><svg ref={svgRef} viewBox={viewBox} preserveAspectRatio="none" onPointerDown={startCanvasPan} onPointerMove={move} onPointerUp={()=>setDrag(null)} onPointerCancel={()=>setDrag(null)} style={{position:"absolute",inset:0,width:"100%",height:"100%",touchAction:"none",cursor:drag?.kind==="pan"?"grabbing":"grab"}}>{image("drone")}{image("map")}
    {sectionList.map(s=>{const q=normalize(parse(sections[s.id]||"")),c=center(q),sel=selectedSection===s.id;return <g key={s.id}><polygon points={stringify(q)} fill={sel?"rgba(124,58,237,.25)":"rgba(124,58,237,.08)"} stroke={sel?"#7c3aed":"#64748b"} strokeWidth={sel?4:2} onPointerDown={e=>startShape(e,s.id,q,"section")}/>{sel&&handlePoints(q).map((h,i)=><circle key={i} cx={h.x} cy={h.y} r={9/zoom} fill="white" stroke="#7c3aed" strokeWidth={3/zoom} style={{cursor:`${h.h}-resize`}} onPointerDown={e=>startResize(e,s.id,h.h,"section")}/>)}<text x={c.x} y={c.y} textAnchor="middle" fontSize={24} fontWeight={800} pointerEvents="none" fill="#172033" paintOrder="stroke" stroke="white" strokeWidth={5}>{s.name}</text></g>})}
    {visiblePlots.map(p=>{const q=normalize(parse(p.points)),sel=selected===p.id,c=center(q);return <g key={p.id}><polygon points={stringify(q)} fill={sel?"rgba(37,99,235,.32)":"rgba(37,99,235,.10)"} stroke={sel?"#1d4ed8":"#334155"} strokeWidth={sel?4:2} onPointerDown={e=>startShape(e,p.id,q,"plot")}/>{sel&&handlePoints(q).map((h,i)=><circle key={i} cx={h.x} cy={h.y} r={10/zoom} fill="white" stroke="#1d4ed8" strokeWidth={3/zoom} style={{cursor:`${h.h}-resize`}} onPointerDown={e=>startResize(e,p.id,h.h,"plot")}/>)}<text x={c.x} y={c.y} textAnchor="middle" dominantBaseline="middle" fontSize={24} fontWeight={800} pointerEvents="none" fill="#172033" paintOrder="stroke" stroke="white" strokeWidth={5}>{p.number}</text></g>})}</svg>{!asset("map")&&!asset("drone")&&<div style={{position:"absolute",inset:0,display:"grid",placeItems:"center",color:"#64748b",pointerEvents:"none"}}>Upload a map or drone image to begin.</div>}</main>
   <aside style={{background:"white",borderLeft:"1px solid #e2e8f0",padding:15,overflow:"auto"}}>{selectedPlot?<><div style={{display:"flex",justifyContent:"space-between"}}><div><small style={{color:"#64748b"}}>SELECTED PLOT</small><h3 style={{margin:"2px 0"}}>Plot {selectedPlot.number}</h3></div><button type="button" onClick={()=>setSelected(null)}><X size={15}/></button></div><div style={{display:"flex",gap:6,marginTop:10}}><button type="button" onClick={duplicatePlot} style={{flex:1}}><Copy size={14}/> Duplicate</button><button type="button" onClick={removePlot} style={{color:"#b91c1c"}}><Trash2 size={14}/> Delete</button></div><div style={{marginTop:14,fontSize:12,fontWeight:800,color:"#64748b"}}>SHAPE</div><div style={{display:"flex",gap:6,marginTop:6}}><button type="button" onClick={()=>convertSelected("rectangle")} style={{flex:1,background:shapeMode==="rectangle"?"#dbeafe":"white"}}>Rectangle · 4</button><button type="button" onClick={()=>convertSelected("polygon")} style={{flex:1,background:shapeMode==="polygon"?"#dbeafe":"white"}}>Polygon · 8</button></div><label style={{display:"block",marginTop:12}}>Plot Number<input value={selectedPlot.number} onChange={e=>updatePlot("number",e.target.value)} style={{width:"100%"}}/></label><label style={{display:"block",marginTop:12}}>Section<select value={selectedPlot.sectionId||""} onChange={e=>updatePlot("sectionId",e.target.value)} style={{width:"100%"}}><option value="">Master Plan</option>{sectionList.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select></label><label style={{display:"block",marginTop:12}}>Status<select value={selectedPlot.status} onChange={e=>updatePlot("status",e.target.value)} style={{width:"100%"}}><option>available</option><option>reserved</option><option>sold</option><option>hold</option></select></label><label style={{display:"block",marginTop:12}}>Owner<input value={selectedPlot.owner} onChange={e=>updatePlot("owner",e.target.value)} style={{width:"100%"}}/></label><label style={{display:"block",marginTop:12}}>Price<input value={selectedPlot.price??""} onChange={e=>updatePlot("price",e.target.value)} style={{width:"100%"}}/></label><label style={{display:"block",marginTop:12}}>Area<input value={selectedPlot.area??""} onChange={e=>updatePlot("area",e.target.value)} style={{width:"100%"}}/></label><label style={{display:"block",marginTop:12}}>Direction<input value={selectedPlot.direction} onChange={e=>updatePlot("direction",e.target.value)} style={{width:"100%"}}/></label><label style={{display:"block",marginTop:12}}>Model<input value={selectedPlot.model} onChange={e=>updatePlot("model",e.target.value)} style={{width:"100%"}}/></label></>:selectedSection?<><small style={{color:"#64748b"}}>SELECTED SECTION</small><h3 style={{margin:"2px 0"}}>{section?.name}</h3><label style={{display:"block",marginTop:14}}>Section name<input value={section?.name||""} onChange={e=>renamePlan(selectedSection,e.target.value)} style={{width:"100%"}}/></label><div style={{marginTop:14,fontSize:12,fontWeight:800,color:"#64748b"}}>SHAPE</div><div style={{display:"flex",gap:6,marginTop:6}}><button type="button" onClick={()=>convertSelected("rectangle")} style={{flex:1,background:shapeMode==="rectangle"?"#dbeafe":"white"}}>Rectangle · 4</button><button type="button" onClick={()=>convertSelected("polygon")} style={{flex:1,background:shapeMode==="polygon"?"#dbeafe":"white"}}>Polygon · 8</button></div><button type="button" onClick={()=>setConfirm({kind:"section",id:selectedSection,label:section?.name||selectedSection})} style={{marginTop:18,width:"100%",padding:9,color:"#b91c1c"}}><Trash2 size={14}/> Delete section</button></>:<div style={{color:"#64748b"}}>Click a plot or section to edit it. Click blank canvas to deselect.</div>}</aside>
  </div>
  {confirm&&<div style={{position:"fixed",inset:0,zIndex:50,display:"grid",placeItems:"center",background:"rgba(15,23,42,.45)"}}><div style={{width:380,background:"white",borderRadius:12,padding:22}}><h3>Delete {confirm.kind}?</h3><p>Are you sure you want to delete <b>{confirm.label}</b>?</p><div style={{display:"flex",justifyContent:"flex-end",gap:8}}><button type="button" onClick={()=>setConfirm(null)}>Cancel</button><button type="button" onClick={()=>confirm.kind==="section"?void deleteSection(confirm.id):(setPlots(v=>v.filter(p=>p.id!==confirm.id)),setSelected(null),setDirty(true),setConfirm(null),setMessage("Plot deleted"))} disabled={Boolean(busy)} style={{background:"#b91c1c",color:"white",border:0,padding:"9px 14px",borderRadius:7}}>Delete</button></div></div></div>}
 </div>;
}
