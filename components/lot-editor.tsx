"use client";
import { ChangeEvent, PointerEvent, useEffect, useMemo, useRef, useState } from "react";
import { Download, Eye, EyeOff, FolderOpen, Grid2X2, Minus, MousePointer2, Move, Plus, Save, Trash2, Upload } from "lucide-react";
import styles from "./lot-editor.module.css";
type Lot={id:string;number:string;status:string;area:number|string|null;price:number|string|null;model:string;owner:string;direction:string;points:string;labelX:number;labelY:number;geometrySource?:string;sectionId?:string|null};
type Section={id:string;name:string;sortOrder:number;masterPlanUrl?:string|null;droneUrl?:string|null};
type Point={x:number;y:number};
function parsePoints(v:string):Point[]{return v.trim().split(/\s+/).map(p=>p.split(",").map(Number)).filter(p=>Number.isFinite(p[0])&&Number.isFinite(p[1])).map(([x,y])=>({x,y}));}
function stringifyPoints(p:Point[]){return p.map(q=>`${Math.round(q.x)},${Math.round(q.y)}`).join(" ");}
function normalizeLots(value:unknown):Lot[]{const source=Array.isArray(value)&&Array.isArray(value[0])?value[0]:value;if(!Array.isArray(source))throw new Error("lots.json must contain an array");return source.map((r:any,i)=>({id:String(r.id??`lot-${i+1}`),number:String(r.number??i+1),status:String(r.status??"available"),area:r.area??null,price:r.price??null,model:String(r.model??""),owner:String(r.owner??""),direction:String(r.direction??""),points:String(r.points??""),labelX:Number(r.labelX??0),labelY:Number(r.labelY??0),geometrySource:r.geometrySource??"manual",sectionId:r.sectionId??null}));}
function bounds(lots:Lot[]){const pts=lots.flatMap(l=>parsePoints(l.points));if(!pts.length)return{minX:0,minY:0,maxX:1600,maxY:1000};return{minX:Math.min(...pts.map(p=>p.x)),minY:Math.min(...pts.map(p=>p.y)),maxX:Math.max(...pts.map(p=>p.x)),maxY:Math.max(...pts.map(p=>p.y))};}
export default function LotEditor({projectSlug}:{projectSlug:string}){
 const [lots,setLots]=useState<Lot[]>([]),[sections,setSections]=useState<Section[]>([]),[selectedSectionId,setSelectedSectionId]=useState(""),[selectedId,setSelectedId]=useState<string|null>(null),[view,setView]=useState<"map"|"drone">("map"),[assetUrl,setAssetUrl]=useState<string|null>(null),[assetName,setAssetName]=useState("No plan uploaded"),[showImage,setShowImage]=useState(true),[tool,setTool]=useState<"select"|"pan"|"draw">("select"),[zoom,setZoom]=useState(1),[ownerColors,setOwnerColors]=useState(false),[dirty,setDirty]=useState(false),[message,setMessage]=useState(""),[draft,setDraft]=useState<Point[]>([]),[drag,setDrag]=useState<{pointIndex?:number;mode:"point"|"lot";start:Point;original:Point[]}|null>(null),[owners,setOwners]=useState<string[]>([]);
 const fileRef=useRef<HTMLInputElement>(null),jsonRef=useRef<HTMLInputElement>(null);const currentSection=sections.find(s=>s.id===selectedSectionId)||sections[0];const selected=lots.find(l=>l.id===selectedId)||null;const filteredLots=useMemo(()=>currentSection?lots.filter(l=>(l.sectionId||currentSection.id)===currentSection.id):[],[lots,currentSection]);const b=useMemo(()=>bounds(filteredLots),[filteredLots]);const viewBox=`${b.minX-100} ${b.minY-100} ${Math.max(1,b.maxX-b.minX+200)} ${Math.max(1,b.maxY-b.minY+200)}`;
 useEffect(()=>{let active=true;fetch(`/api/projects/${encodeURIComponent(projectSlug)}/plan`,{cache:"no-store"}).then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.error||"Unable to load plan");return d;}).then(d=>{if(!active)return;setSections(d.sections||[]);setLots(d.lots||[]);setSelectedSectionId(d.sections?.[0]?.id||"");setOwners(Array.from(new Set((d.lots||[]).map((l:any)=>String(l.owner||"").trim()).filter(Boolean))) as string[]);}).catch(e=>active&&setMessage(e instanceof Error?e.message:"Unable to load plan"));return()=>{active=false};},[projectSlug]);
 useEffect(()=>{if(!currentSection){setAssetUrl(null);setAssetName("No plan uploaded");return;}const source=view==="map"?currentSection.masterPlanUrl:currentSection.droneUrl;setAssetUrl(source?`/api/projects/${encodeURIComponent(projectSlug)}/assets/file?kind=${view==="map"?"section-master-plan":"section-drone"}&section=${currentSection.id}&v=${Date.now()}`:null);setAssetName(source?`${currentSection.name} ${view}`:`No ${view} uploaded`);setSelectedId(null);setDraft([]);setZoom(1);},[currentSection,view,projectSlug]);
 function updateLot(id:string,patch:Partial<Lot>){setLots(p=>p.map(l=>l.id===id?{...l,...patch}:l));setDirty(true);}function selectLot(id:string){setSelectedId(id);setTool("select");setZoom(3);}
 async function save(){try{if(!sections.length)throw new Error("No sections loaded");const r=await fetch(`/api/projects/${encodeURIComponent(projectSlug)}/plan`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({sections,lots})});const d=await r.json();if(!r.ok)throw new Error(d.error||"Unable to save");const refreshed=await fetch(`/api/projects/${encodeURIComponent(projectSlug)}/plan`,{cache:"no-store"}).then(x=>x.json());if(!refreshed.sections)throw new Error(refreshed.error||"Unable to reload plan");setSections(refreshed.sections);setLots(refreshed.lots||[]);setSelectedSectionId(prev=>refreshed.sections.some((s:any)=>s.id===prev)?prev:refreshed.sections[0]?.id||"");setOwners(Array.from(new Set((refreshed.lots||[]).map((l:any)=>String(l.owner||"").trim()).filter(Boolean))) as string[]);setDirty(false);setMessage("Saved");setTimeout(()=>setMessage(""),1800);}catch(e){setMessage(e instanceof Error?e.message:"Unable to save");}}
 function addSection(){const n=sections.filter(s=>s.id!=="master").length+1;const s:Section={id:crypto.randomUUID(),name:`Section ${n}`,sortOrder:sections.length,masterPlanUrl:null,droneUrl:null};setSections(p=>[...p,s]);setSelectedSectionId(s.id);setView("map");setDirty(true);setMessage(`${s.name} added — upload its map and drone image`);}
 async function uploadAsset(e:ChangeEvent<HTMLInputElement>){const file=e.target.files?.[0];if(!file||!currentSection)return;if(!/image\/(svg\+xml|png|jpeg|jpg|webp)/i.test(file.type)&&!/\.(svg|png|jpe?g|webp)$/i.test(file.name)){setMessage("Use SVG, JPG, PNG or WebP");return;}try{const prep=await fetch(`/api/projects/${encodeURIComponent(projectSlug)}/plan`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({sections,lots})});const prepData=await prep.json();if(!prep.ok)throw new Error(prepData.error||"Unable to save section before upload");const fresh=await fetch(`/api/projects/${encodeURIComponent(projectSlug)}/plan`,{cache:"no-store"}).then(r=>r.json());const persistedSection=fresh?.sections?.find((s:any)=>s.id===currentSection.id)||currentSection;if(fresh?.sections){setSections(fresh.sections);setSelectedSectionId(persistedSection.id);}const form=new FormData();form.append("file",file);form.append("sectionId",persistedSection.id);form.append("kind",view==="map"?"section-master-plan":"section-drone");const r=await fetch(`/api/projects/${encodeURIComponent(projectSlug)}/assets`,{method:"POST",body:form});const d=await r.json();if(!r.ok)throw new Error(d.error||"Upload failed");setSections(p=>p.map(s=>s.id===persistedSection.id?{...s,...(view==="map"?{masterPlanUrl:d.savedValue}:{droneUrl:d.savedValue})}:s));setAssetUrl(`/api/projects/${encodeURIComponent(projectSlug)}/assets/file?kind=${view==="map"?"section-master-plan":"section-drone"}&section=${persistedSection.id}&v=${Date.now()}`);setAssetName(file.name);setDirty(false);setMessage(`${view==="map"?"Map":"Drone image"} uploaded`);}catch(e){setMessage(e instanceof Error?e.message:"Upload failed");}e.target.value="";}
 function importLots(e:ChangeEvent<HTMLInputElement>){const file=e.target.files?.[0];if(!file)return;const reader=new FileReader();reader.onload=()=>{try{const imported=normalizeLots(JSON.parse(String(reader.result))).map(l=>({...l,sectionId:l.sectionId||selectedSectionId}));setLots(p=>[...p.filter(l=>(l.sectionId||selectedSectionId)!==selectedSectionId),...imported]);setOwners(p=>Array.from(new Set([...p,...imported.map(l=>l.owner).filter(Boolean)])));setDirty(true);setMessage("lots.json imported");}catch{setMessage("Invalid lots.json");}};reader.readAsText(file);e.target.value="";}
 function exportLots(){const a=document.createElement("a"),u=URL.createObjectURL(new Blob([JSON.stringify(lots,null,2)],{type:"application/json"}));a.href=u;a.download="lots.json";a.click();URL.revokeObjectURL(u);}
 function svgPoint(e:PointerEvent<SVGSVGElement>){const pt=e.currentTarget.createSVGPoint();pt.x=e.clientX;pt.y=e.clientY;const m=e.currentTarget.getScreenCTM();if(!m)return{x:0,y:0};const p=pt.matrixTransform(m.inverse());return{x:p.x,y:p.y};}
 function down(e:PointerEvent<SVGSVGElement>){const p=svgPoint(e);if(tool==="draw"){setDraft(q=>[...q,p]);return;}if(!selected)return;const points=parsePoints(selected.points);const hit=points.findIndex(q=>Math.hypot(q.x-p.x,q.y-p.y)<18/zoom);if(hit>=0)setDrag({pointIndex:hit,mode:"point",start:p,original:points});else if(p.x>=Math.min(...points.map(q=>q.x))&&p.x<=Math.max(...points.map(q=>q.x))&&p.y>=Math.min(...points.map(q=>q.y))&&p.y<=Math.max(...points.map(q=>q.y)))setDrag({mode:"lot",start:p,original:points});}
 function move(e:PointerEvent<SVGSVGElement>){if(!drag||!selected)return;const p=svgPoint(e),dx=p.x-drag.start.x,dy=p.y-drag.start.y;let next=drag.original.map(q=>({...q}));if(drag.mode==="point"&&drag.pointIndex!==undefined)next[drag.pointIndex]=p;else next=next.map(q=>({x:q.x+dx,y:q.y+dy}));updateLot(selected.id,{points:stringifyPoints(next),labelX:selected.labelX+(drag.mode==="lot"?dx:0),labelY:selected.labelY+(drag.mode==="lot"?dy:0),geometrySource:"manual"});setDrag({...drag,start:p,original:next});}
 function finishLot(){if(draft.length<3||!currentSection)return;const n=String(Math.max(0,...lots.map(l=>Number(l.number)).filter(Number.isFinite))+1),c=draft.reduce((a,p)=>({x:a.x+p.x/draft.length,y:a.y+p.y/draft.length}),{x:0,y:0});const l:Lot={id:`lot-${Date.now()}`,number:n,status:"available",area:null,price:null,model:"",owner:"",direction:"",points:stringifyPoints(draft),labelX:c.x,labelY:c.y,geometrySource:"manual",sectionId:currentSection.id};setLots(p=>[...p,l]);setSelectedId(l.id);setDraft([]);setTool("select");setDirty(true);}
 function ownerPlus(){const name=window.prompt("Owner name")?.trim();if(!name)return;setOwners(p=>Array.from(new Set([...p,name])));if(selected)updateLot(selected.id,{owner:name});setMessage(`${name} added`);}
 function ownerFill(l:Lot){if(!ownerColors||!l.owner)return undefined;let h=0;for(const c of l.owner)h=(h*31+c.charCodeAt(0))%360;return`hsl(${h} 70% 55% / .45)`;}
 return <div className={styles.editor}><header className={styles.header}><div><div className={styles.eyebrow}>LANDGRID / MAP AND MANAGE</div><h1>Lot Editor</h1><span>{currentSection?.name||"Master"} · {assetName}</span></div><div className={styles.actions}><button onClick={()=>jsonRef.current?.click()}><FolderOpen size={16}/> Import lots</button><button onClick={exportLots}><Download size={16}/> Export lots</button><button className={styles.primary} onClick={save}><Save size={16}/> Save{dirty?" *":""}</button></div></header>
 <div className={styles.toolbar}><label>Section <select value={selectedSectionId} onChange={e=>{setSelectedSectionId(e.target.value);setSelectedId(null);}}>{sections.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select></label><button onClick={addSection}><Plus size={16}/> Add section</button><span className={styles.separator}/><button className={tool==="select"?styles.active:""} onClick={()=>setTool("select")}><MousePointer2 size={17}/></button><button className={tool==="pan"?styles.active:""} onClick={()=>setTool("pan")}><Move size={17}/></button><button className={tool==="draw"?styles.active:""} onClick={()=>setTool("draw")}><Grid2X2 size={17}/></button><span className={styles.separator}/><button onClick={()=>setZoom(z=>Math.min(8,z+.25))}><Plus size={17}/></button><strong>{Math.round(zoom*100)}%</strong><button onClick={()=>setZoom(z=>Math.max(.25,z-.25))}><Minus size={17}/></button><button onClick={()=>setZoom(1)}>Fit</button><span className={styles.separator}/><button className={view==="map"?styles.active:""} onClick={()=>setView("map")}>Map</button><button className={view==="drone"?styles.active:""} onClick={()=>setView("drone")}>Drone</button><button onClick={()=>fileRef.current?.click()}><Upload size={16}/> Upload {view}</button><button onClick={()=>setShowImage(v=>!v)}>{showImage?<EyeOff size={16}/>:<Eye size={16}/>} {showImage?"Hide image":"Show image"}</button><button className={ownerColors?styles.active:""} onClick={()=>setOwnerColors(v=>!v)}>Owner colors</button><span className={styles.spacer}/><span className={styles.hint}>{message||"Select a lot to edit"}</span></div>
 <main className={styles.body}><aside className={styles.sidebar}><div className={styles.sideTop}><div><b>Lots</b><small>{filteredLots.length} in {currentSection?.name||"Master"}</small></div></div><div className={styles.lotList}>{filteredLots.map(l=><button key={l.id} className={`${styles.lotCard} ${selectedId===l.id?styles.selected:""}`} onClick={()=>selectLot(l.id)}><span className={styles.lotNumber}>{l.number}</span><span><b>Lot {l.number}</b><small>{l.status}{l.owner?` · ${l.owner}`:""}</small></span></button>)}</div></aside>
 <section className={styles.stage}><div className={styles.canvasWrap}><div className={styles.canvas}>{assetUrl&&showImage?<img src={assetUrl} alt={`${currentSection?.name||"Master"} ${view}`} style={{position:"absolute",inset:0,width:"100%",height:"100%",objectFit:"fill",pointerEvents:"none"}}/>:<div className={styles.empty}>Upload a {view==='map'?'site/master plan':'drone image'} for this section.</div>}<svg viewBox={viewBox} preserveAspectRatio="none" style={{position:"absolute",inset:0,width:"100%",height:"100%"}} onPointerDown={down} onPointerMove={move} onPointerUp={()=>setDrag(null)}>{filteredLots.map(l=><g key={l.id} onClick={()=>selectLot(l.id)}><polygon points={l.points} fill={ownerFill(l)||(selectedId===l.id?"rgba(37,99,235,.35)":"rgba(37,99,235,.08)")} stroke={selectedId===l.id?"#2457d6":"#fff"} strokeWidth={selectedId===l.id?4:2}/><text x={l.labelX} y={l.labelY} textAnchor="middle" dominantBaseline="middle" fontSize="24" fontWeight="800" fill="#172033" paintOrder="stroke" stroke="#fff" strokeWidth="5">{l.number}</text></g>)}{draft.length>0&&<polyline points={draft.map(p=>`${p.x},${p.y}`).join(" ")} fill="none" stroke="#2457d6" strokeWidth="4"/>}</svg>{tool==='draw'&&draft.length>=3&&<button className={styles.finish} onClick={finishLot}>Finish lot</button>}</div></div></section>
 <aside className={styles.inspector}>{selected?<><div className={styles.inspectorHead}><div><small>LOT DETAILS</small><h2>Lot {selected.number}</h2></div></div><label>Status<select value={selected.status} onChange={e=>updateLot(selected.id,{status:e.target.value})}><option>available</option><option>reserved</option><option>sold</option><option>pending</option><option>hold</option></select></label><label>Owner<div style={{display:"flex",gap:6}}><select style={{flex:1}} value={selected.owner} onChange={e=>updateLot(selected.id,{owner:e.target.value})}><option value="">No owner</option>{owners.map(o=><option key={o} value={o}>{o}</option>)}</select><button type="button" title="Add owner" onClick={ownerPlus}><Plus size={16}/></button></div></label><label>Area<input value={selected.area??""} onChange={e=>updateLot(selected.id,{area:e.target.value})}/></label><label>Price<input value={selected.price??""} onChange={e=>updateLot(selected.id,{price:e.target.value})}/></label><label>Model<input value={selected.model} onChange={e=>updateLot(selected.id,{model:e.target.value})}/></label><label>Direction<input value={selected.direction} onChange={e=>updateLot(selected.id,{direction:e.target.value})}/></label><button className={styles.primary} onClick={save}><Save size={16}/> Save lot</button><button className={styles.deleteButton} onClick={()=>{setLots(p=>p.filter(l=>l.id!==selected.id));setSelectedId(null);setDirty(true);}}><Trash2 size={15}/> Delete lot</button></>:<div className={styles.noSelection}><h3>Select a lot</h3><p>Choose a lot from the map or list to edit its status, owner, area, price and other attributes.</p></div>}</aside></main>
 <input ref={fileRef} type="file" hidden accept=".svg,.png,.jpg,.jpeg,.webp,image/svg+xml,image/png,image/jpeg,image/webp" onChange={uploadAsset}/><input ref={jsonRef} type="file" hidden accept=".json,application/json" onChange={importLots}/></div>;
}
