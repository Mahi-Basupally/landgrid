'use client';

import { useEffect, useMemo, useState } from 'react';
import { Check, ChevronDown, FileDown, Users, X } from 'lucide-react';

type Owner = { id: string; name: string; plotNumbers: string[]; color?: string | null };
type Lot = { id: string; number: string; ownerId?: string | null; points: string; area?: number | null };
type Palette = { base: string; light: string; dark: string };

const paletteFor = (i: number): Palette => { const h = Math.round((i * 137.508) % 360); return { base: `hsl(${h} 78% 52%)`, light: `hsl(${h} 88% 82%)`, dark: `hsl(${h} 82% 34%)` }; };
const hexToPalette = (hex: string): Palette => { const m = /^#([0-9a-f]{6})$/i.exec(hex); if (!m) return { base:'#2563eb',light:'#bfdbfe',dark:'#1d4ed8' }; const n=parseInt(m[1],16),r=(n>>16)&255,g=(n>>8)&255,b=n&255; return { base:hex.toUpperCase(),light:`rgb(${Math.min(255,r+90)},${Math.min(255,g+90)},${Math.min(255,b+90)})`,dark:`rgb(${Math.round(r*.65)},${Math.round(g*.65)},${Math.round(b*.65)})` }; };
const lotCenter=(points:string)=>{const p=points.trim().split(/\s+/).map(v=>v.split(',').map(Number)).filter(v=>Number.isFinite(v[0])&&Number.isFinite(v[1]));return p.length?{x:p.reduce((s,v)=>s+v[0],0)/p.length,y:p.reduce((s,v)=>s+v[1],0)/p.length}:null;};
const fmtYards=(v:number|null|undefined)=>v!=null&&Number.isFinite(Number(v))?Number(v).toLocaleString(undefined,{maximumFractionDigits:2}):'—';
const esc=(v:unknown)=>String(v??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#39;');

export default function OwnerFilter({ projectSlug }: { projectSlug:string }) {
  const [owners,setOwners]=useState<Owner[]>([]),[lots,setLots]=useState<Lot[]>([]),[selected,setSelected]=useState<Set<string>>(new Set()),[open,setOpen]=useState(true),[loading,setLoading]=useState(true),[exporting,setExporting]=useState(false);

  useEffect(()=>{let cancelled=false;setLoading(true);fetch(`/api/projects/${encodeURIComponent(projectSlug)}/plan`,{cache:'no-store'}).then(r=>r.json()).then(d=>{if(cancelled)return;const rawOwners=d.owners||d.project_owners||[],byId=new Map<string,Owner>();rawOwners.forEach((o:any)=>byId.set(String(o.id),{id:String(o.id),name:String(o.name||'Unnamed owner'),plotNumbers:[],color:o.color||null}));const rawLots:Lot[]=(d.lots||d.plots||[]).map((l:any)=>({id:String(l.id),number:String(l.number??l.plot_number??''),ownerId:l.ownerId!=null?String(l.ownerId):(l.owner_id!=null?String(l.owner_id):null),area:l.area!=null?Number(l.area):(l.area_sq_yards!=null?Number(l.area_sq_yards):null),points:typeof l.points==='string'?l.points:String(l.points||l.geometry?.points||'')}));const map=new Map<string,Owner>();rawLots.forEach(l=>{if(!l.ownerId)return;const o=byId.get(l.ownerId);if(!o)return;const cur=map.get(o.id)||{...o,plotNumbers:[]};cur.plotNumbers.push(l.number);map.set(o.id,cur);});setLots(rawLots);setOwners([...map.values()].sort((a,b)=>a.name.localeCompare(b.name)));}).catch(()=>{if(!cancelled){setOwners([]);setLots([]);}}).finally(()=>{if(!cancelled)setLoading(false);});return()=>{cancelled=true;};},[projectSlug]);

  const colorByOwner=useMemo(()=>{const m=new Map<string,Palette>();owners.forEach((o,i)=>m.set(o.id,o.color?hexToPalette(o.color):paletteFor(i)));return m;},[owners]);

  useEffect(()=>{let disposed=false,timer=0,raf=0;let observer:MutationObserver|null=null;const findPolygon=(svg:SVGSVGElement,lot:Lot)=>{const wanted=lot.number.trim();if(!wanted)return null;for(const t of Array.from(svg.querySelectorAll('text')) as SVGTextElement[]){if((t.textContent||'').trim()!==wanted||t.hasAttribute('data-owner-yard-label'))continue;const p=t.closest('g')?.querySelector('polygon') as SVGPolygonElement|null;if(p)return p;}return null;};const apply=()=>{if(disposed)return;const svg=(Array.from(document.querySelectorAll('.pv-canvas svg')) as SVGSVGElement[]).find(s=>s.querySelector('polygon'));if(!svg||!lots.length){timer=window.setTimeout(apply,100);return;}const polys=new Map<string,SVGPolygonElement>();lots.forEach(l=>{const p=findPolygon(svg,l);if(p)polys.set(l.id,p);});observer?.disconnect();polys.forEach(p=>{p.style.removeProperty('fill');p.style.removeProperty('fill-opacity');p.style.removeProperty('stroke');p.style.removeProperty('stroke-width');p.style.removeProperty('filter');});svg.querySelectorAll('[data-owner-yard-label]').forEach(e=>e.remove());selected.forEach(ownerId=>{const pal=colorByOwner.get(ownerId)||paletteFor(0);lots.filter(l=>String(l.ownerId)===ownerId).forEach(lot=>{const p=polys.get(lot.id);if(!p)return;p.style.setProperty('fill',pal.base,'important');p.style.setProperty('fill-opacity','.90','important');p.style.setProperty('stroke','#fff','important');p.style.setProperty('stroke-width','5','important');p.style.setProperty('filter',`drop-shadow(0 0 6px ${pal.base})`,'important');const c=lotCenter(lot.points);if(!c||lot.area==null)return;const label=document.createElementNS('http://www.w3.org/2000/svg','text');label.setAttribute('data-owner-yard-label','true');label.setAttribute('x',String(c.x));label.setAttribute('y',String(c.y+18));label.setAttribute('text-anchor','middle');label.setAttribute('dominant-baseline','middle');label.setAttribute('font-size','13');label.setAttribute('font-weight','800');label.setAttribute('pointer-events','none');label.setAttribute('fill','#172033');label.setAttribute('paint-order','stroke');label.setAttribute('stroke','white');label.setAttribute('stroke-width','4');label.textContent=`${fmtYards(lot.area)} sq.yd`;p.parentElement?.appendChild(label);});});if(!disposed&&observer)observer.observe(document.querySelector('.pv-canvas')!,{childList:true,subtree:true});};const schedule=()=>{if(raf)return;raf=requestAnimationFrame(()=>{raf=0;apply();});};const canvas=document.querySelector('.pv-canvas');observer=canvas?new MutationObserver(ms=>{const onlyLabels=ms.length>0&&ms.every(m=>[...m.addedNodes,...m.removedNodes].every(n=>(n as Element).nodeType!==1||(n as Element).hasAttribute?.('data-owner-yard-label')));if(!onlyLabels)schedule();}):null;observer?.observe(canvas!,{childList:true,subtree:true});schedule();return()=>{disposed=true;observer?.disconnect();if(raf)cancelAnimationFrame(raf);if(timer)clearTimeout(timer);};},[lots,selected,colorByOwner]);

  const toggle=(id:string)=>setSelected(s=>{const n=new Set(s);n.has(id)?n.delete(id):n.add(id);return n;});
  const clear=()=>setSelected(new Set());
  async function changeColor(id:string,color:string){const c=/^#[0-9a-f]{6}$/i.test(color)?color.toUpperCase():color;setOwners(os=>os.map(o=>o.id===id?{...o,color:c}:o));try{await fetch(`/api/projects/${encodeURIComponent(projectSlug)}/owners`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id,color:c})});}catch{}}

  const selectedSummary=useMemo(()=>owners.filter(o=>selected.has(o.id)).map(owner=>{const ownerLots=lots.filter(l=>String(l.ownerId)===owner.id);return{owner,ownerLots,totalYards:ownerLots.reduce((s,l)=>s+(Number.isFinite(Number(l.area))?Number(l.area):0),0)};}),[owners,lots,selected]);
  const selectedPlotCount=selectedSummary.reduce((s,x)=>s+x.ownerLots.length,0),selectedYards=selectedSummary.reduce((s,x)=>s+x.totalYards,0);

  function exportPdf(){if(!selectedSummary.length)return;setExporting(true);try{const source=(Array.from(document.querySelectorAll('.pv-canvas svg')) as SVGSVGElement[]).find(s=>s.querySelector('polygon'));if(!source)throw new Error('Map is not ready');const clone=source.cloneNode(true) as SVGSVGElement;clone.querySelectorAll('[data-owner-yard-label]').forEach(e=>e.remove());const viewBox=source.getAttribute('viewBox');if(viewBox)clone.setAttribute('viewBox',viewBox);clone.setAttribute('preserveAspectRatio','xMidYMid meet');clone.setAttribute('width','100%');clone.setAttribute('height','auto');clone.querySelectorAll('[style]').forEach(el=>{const e=el as HTMLElement;e.style.removeProperty('transform');e.style.removeProperty('zoom');});const rows=selectedSummary.flatMap(({owner,ownerLots})=>ownerLots.map(l=>`<tr><td><span class="sw" style="background:${esc(colorByOwner.get(owner.id)?.base||'#2563eb')}"></span>${esc(owner.name)}</td><td>${esc(l.number)}</td><td class="num">${fmtYards(l.area)}</td></tr>`));const subtotals=selectedSummary.map(({owner,totalYards})=>`<tr class="subtotal"><td colspan="2"><span class="sw" style="background:${esc(colorByOwner.get(owner.id)?.base||'#2563eb')}"></span>${esc(owner.name)} subtotal</td><td class="num">${fmtYards(totalYards)} sq.yd</td></tr>`).join('');const legend=selectedSummary.map(({owner,ownerLots,totalYards})=>`<div class="owner"><i class="sw" style="background:${esc(colorByOwner.get(owner.id)?.base||'#2563eb')}"></i><b>${esc(owner.name)}</b><span>${ownerLots.length} plot${ownerLots.length===1?'':'s'} · ${fmtYards(totalYards)} sq.yd</span></div>`).join('');const w=window.open('','_blank');if(!w)throw new Error('Popup blocked. Please allow popups for this site.');w.document.write(`<!doctype html><html><head><title>LandGrid Owner Map</title><style>@page{size:landscape;margin:10mm}*{box-sizing:border-box}body{font-family:Arial,Helvetica,sans-serif;color:#172033;margin:0;font-size:10px}h1{font-size:20px;margin:0 0 4px}h2{font-size:13px;margin:0 0 7px}.meta{font-size:10px;color:#64748b;margin:0 0 9px}.map{border:1px solid #cbd5e1;border-radius:6px;padding:6px;background:#fff;height:65vh;display:flex;align-items:center;justify-content:center}.map svg{display:block;width:100%;height:100%;max-width:100%;max-height:100%}.legend{margin-top:9px;border:1px solid #dbe3ee;border-radius:6px;padding:8px}.legend-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:5px 12px}.owner{display:flex;align-items:center;gap:5px;min-width:0}.owner span{color:#64748b;white-space:nowrap}.sw{display:inline-block;width:9px;height:9px;border-radius:50%;flex:0 0 auto} .details{page-break-before:always}.cards{display:flex;gap:8px;margin-bottom:10px}.card{border:1px solid #dbe3ee;border-radius:6px;padding:7px 10px;min-width:120px}.card b{display:block;font-size:15px}.card span{font-size:9px;color:#64748b;text-transform:uppercase;font-weight:700}table{width:100%;border-collapse:collapse;font-size:10px}thead{display:table-header-group}th,td{padding:5px 7px;border-bottom:1px solid #e5e7eb;text-align:left}th{background:#f1f5f9;color:#475569;font-size:9px;text-transform:uppercase}.num{text-align:right;font-variant-numeric:tabular-nums}.subtotal{background:#f8fafc;font-weight:800;page-break-inside:avoid}.grand{background:#e2e8f0;font-weight:900;font-size:11px;page-break-inside:avoid}.note{font-size:9px;color:#94a3b8;margin-top:8px}</style></head><body><section><h1>LandGrid — Owner Highlight Map</h1><p class="meta">Complete site plan · ${selectedPlotCount} selected plots · ${fmtYards(selectedYards)} sq.yd · ${esc(new Date().toLocaleString())}</p><div class="map">${clone.outerHTML}</div><div class="legend"><h2>Selected Owner Legend</h2><div class="legend-grid">${legend}</div></div></section><section class="details"><h1>Plot Summary</h1><p class="meta">Selected owner plots and areas</p><div class="cards"><div class="card"><span>Total plots</span><b>${selectedPlotCount}</b></div><div class="card"><span>Total sq.yd</span><b>${fmtYards(selectedYards)}</b></div><div class="card"><span>Owners</span><b>${selectedSummary.length}</b></div></div><table><thead><tr><th>Owner</th><th>Plot</th><th class="num">Area (sq.yd)</th></tr></thead><tbody>${rows.join('')}${subtotals}<tr class="grand"><td colspan="2">Overall Total</td><td class="num">${fmtYards(selectedYards)} sq.yd</td></tr></tbody></table><div class="note">Generated from the current LandGrid site plan.</div></section><script>window.onload=()=>setTimeout(()=>window.print(),350)</script></body></html>`);w.document.close();}catch(e){window.alert(e instanceof Error?e.message:'Unable to export the map.');}finally{setExporting(false);}}

  return <div className="landgrid-owner-filter" style={{position:'absolute',top:0,right:0,width:350,maxWidth:'min(350px,calc(100% - 16px))',maxHeight:'calc(100% - 16px)',overflow:'hidden',zIndex:30,background:'rgba(255,255,255,.98)',border:'1px solid #dbe3ee',borderTop:0,borderRight:0,borderRadius:'0 0 0 12px',boxShadow:'0 8px 28px rgba(15,23,42,.14)',backdropFilter:'blur(12px)',display:'flex',flexDirection:'column'}}>
    <div style={{display:'flex',alignItems:'center',justifyContent:'space-between',padding:'12px 14px 10px',flex:'0 0 auto'}}><button onClick={()=>setOpen(v=>!v)} style={{border:0,background:'transparent',padding:0,display:'flex',alignItems:'center',gap:8,cursor:'pointer',color:'#172033'}}><span style={{width:28,height:28,borderRadius:8,display:'grid',placeItems:'center',background:'#eff6ff',color:'#2563eb'}}><Users size={15}/></span><span style={{textAlign:'left'}}><span style={{display:'block',fontSize:11,letterSpacing:.8,fontWeight:900,color:'#64748b'}}>OWNERS</span><span style={{display:'block',fontSize:13,fontWeight:850}}>Highlight by owner</span></span><ChevronDown size={15} style={{marginLeft:2,transform:open?'rotate(180deg)':undefined}}/></button>{selected.size>0&&<button onClick={clear} style={{border:0,background:'#f8fafc',color:'#475569',borderRadius:7,padding:'5px 7px',cursor:'pointer',fontSize:10,fontWeight:800,display:'inline-flex',alignItems:'center',gap:4}}><X size={12}/>Clear</button>}</div>
    {open&&<div style={{overflowY:'auto',padding:'7px 10px 0',flex:'1 1 auto'}}><div style={{fontSize:9,fontWeight:900,letterSpacing:.8,color:'#94a3b8',padding:'2px 7px 6px'}}>OWNERS</div>{loading?<div style={{padding:12,color:'#94a3b8',fontSize:11}}>Loading owners…</div>:owners.length===0?<div style={{padding:12,color:'#94a3b8',fontSize:11}}>No plot owners assigned.</div>:owners.map((owner,i)=>{const pal=owner.color?hexToPalette(owner.color):paletteFor(i),checked=selected.has(owner.id),hex=/^#[0-9a-f]{6}$/i.test(owner.color||'')?owner.color!.toUpperCase():'#2563EB';return <div key={owner.id} style={{display:'flex',alignItems:'center',gap:8,padding:7,borderRadius:8,background:checked?`${pal.light}66`:'transparent',border:checked?`1px solid ${pal.light}`:'1px solid transparent'}}><button type="button" onClick={()=>toggle(owner.id)} aria-label={`Highlight ${owner.name}`} style={{width:18,height:18,borderRadius:5,display:'grid',placeItems:'center',border:`1.5px solid ${checked?pal.dark:'#cbd5e1'}`,background:checked?pal.base:'#fff',color:'#fff',cursor:'pointer',padding:0}}>{checked&&<Check size={12} strokeWidth={3}/>}</button><input aria-label={`Color for ${owner.name}`} type="color" value={hex} onChange={e=>void changeColor(owner.id,e.target.value)} style={{width:24,height:24,padding:0,border:0,borderRadius:5,cursor:'pointer'}}/><span style={{minWidth:0,flex:1,fontSize:12,fontWeight:750,color:'#243047',overflow:'hidden',textOverflow:'ellipsis',whiteSpace:'nowrap'}}>{owner.name}</span><span style={{fontSize:10,color:'#64748b',fontWeight:700}}>{owner.plotNumbers.length}</span></div>;})}
      {selectedSummary.length>0&&<div style={{margin:'10px 0 8px',borderTop:'1px solid #e2e8f0',paddingTop:10}}><div style={{display:'flex',alignItems:'flex-end',justifyContent:'space-between',padding:'0 7px 7px'}}><div><div style={{fontSize:9,fontWeight:900,letterSpacing:.8,color:'#94a3b8'}}>PLOT SUMMARY</div><div style={{fontSize:13,fontWeight:900,color:'#172033'}}>Selected owners</div></div><div style={{textAlign:'right'}}><div style={{fontSize:9,color:'#64748b',fontWeight:800}}>{selectedPlotCount} plots</div><div style={{fontSize:12,fontWeight:900}}>{fmtYards(selectedYards)} sq.yd</div></div></div><div style={{border:'1px solid #e2e8f0',borderRadius:9,overflow:'hidden',background:'#fff'}}>{selectedSummary.map(({owner,ownerLots,totalYards})=><div key={owner.id} style={{padding:'8px 9px',borderTop:'1px solid #eef2f7'}}><div style={{display:'flex',alignItems:'center',gap:6}}><span style={{width:9,height:9,borderRadius:99,background:colorByOwner.get(owner.id)?.base||'#2563eb',flex:'0 0 auto'}}/><span style={{fontSize:11,fontWeight:850,color:'#243047',minWidth:0,flex:1,overflow:'hidden',textOverflow:'ellipsis',whiteSpace:'nowrap'}}>{owner.name}</span><span style={{fontSize:9,color:'#64748b',fontWeight:800}}>{ownerLots.length} plots</span><span style={{fontSize:10,fontWeight:900}}>{fmtYards(totalYards)}</span></div><div style={{marginTop:5,paddingLeft:15,display:'flex',flexWrap:'wrap',gap:'4px 7px'}}>{ownerLots.map(l=><span key={l.id} style={{fontSize:9,color:'#475569',background:'#f8fafc',border:'1px solid #e2e8f0',borderRadius:5,padding:'3px 5px',fontVariantNumeric:'tabular-nums'}}>{l.number} · {fmtYards(l.area)} yd²</span>)}</div></div>)}</div></div>}
    </div>}
    {selected.size>0&&<div style={{padding:'9px 10px 10px',borderTop:'1px solid #e2e8f0',background:'#fff',flex:'0 0 auto'}}><button type="button" onClick={exportPdf} disabled={exporting} style={{width:'100%',border:0,background:exporting?'#94a3b8':'#172033',color:'#fff',borderRadius:8,padding:'10px 11px',cursor:exporting?'wait':'pointer',display:'inline-flex',alignItems:'center',justifyContent:'center',gap:6,fontSize:11,fontWeight:850}}><FileDown size={14}/>{exporting?'Preparing PDF…':'Export map + plot summary to PDF'}</button><div style={{fontSize:9,color:'#94a3b8',textAlign:'center',marginTop:5}}>Complete map + selected owner details</div></div>}
  </div>;
}
